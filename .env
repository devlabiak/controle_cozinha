# ==================== CONFIGURAÇÕES DE PRODUÇÃO ====================
# Arquivo .env - Variáveis de Ambiente para Controle de Cozinha
# Gerado em: 2026-02-05 20:32:38
# IMPORTANTE: Este arquivo contém dados sensíveis. Nunca commitar em produção!
# Usar .env.example no repositório como template.

# ==================== BANCO DE DADOS ====================
# Conexão com PostgreSQL
# Em produção, usar senha forte e host remoto
DATABASE_URL=postgresql://postgres:postgres_db_2026@db:5432/controle_cozinha

# ==================== SEGURANÇA - CHAVE SECRETA ====================
# SECRET_KEY definitiva para assinatura de JWTs
# Gerada com secrets.token_urlsafe(64) - 86 caracteres
# NUNCA usar a chave padrão em produção!
# Para regenerar: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=7re10TCfrJiu-Geui6ypHF0A6HClRrxAIgbdLdREyMIfc6M6eGL3lGwx29CzBZG72lTrwN13oRqx0RycmLJXfQ
ALGORITHM=HS256

# ==================== TOKENS DE AUTENTICAÇÃO ====================
# Tempo de expiração do token de acesso (em minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Tempo de expiração do refresh token (em dias)
REFRESH_TOKEN_EXPIRE_DAYS=7

# ==================== DOMÍNIO ====================
# Domínio base para identificação de subdomínios (tenants)
BASE_DOMAIN=wlsolucoes.eti.br

# ==================== CORS - ORIGENS PERMITIDAS ====================
# Domínios que podem fazer requisições (separados por vírgula)
# Em produção, usar apenas https://
ALLOWED_ORIGINS=https://painelfood.wlsolucoes.eti.br,https://cozinha.wlsolucoes.eti.br,https://admin.wlsolucoes.eti.br

# ==================== COOKIES - CONFIGURAÇÃO SEGURA ====================
# Ativar em PRODUÇÃO para força transmissão apenas via HTTPS
COOKIE_SECURE=true

# Política SameSite para proteção contra CSRF
# Opções: strict, lax, none
# Em produção: strict (mais seguro, mas pode quebrar integrations)
COOKIE_SAMESITE=strict

# ==================== LOGGING ====================
# Nível de log: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Em produção: INFO ou WARNING
LOG_LEVEL=INFO

# ==================== REDIS - CACHE E BLACKLIST DE TOKENS ====================
# URL de conexão com Redis (para revogação de tokens)
# Formato: redis://[:password]@host:port/db
# Exemplo remoto: redis://:senha@redis-host:6379/0
REDIS_URL=redis://localhost:6379/0

# ==================== SETTINGS ADICIONAIS ====================
# Enable HTTPS redirect (em produção via nginx)
ENABLE_HTTPS_REDIRECT=true

# Rate limiting por IP
RATE_LIMIT_LOGIN=20/minute

# Retenção de histórico (dias)
HISTORY_RETENTION_DAYS=90
